#include <stdio.h>
int main(int argc, char const *argv[])
{ 
    int a,b;


    if a = "%d"
    //body
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    a X a = b; 
    return 0;
}
